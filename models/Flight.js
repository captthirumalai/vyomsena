export class Flight {
  constructor(id, flightNumber, origin, destination) {
    this.id = id;
    this.flightNumber = flightNumber;
    this.origin = origin;
    this.destination = destination;
  }
}
